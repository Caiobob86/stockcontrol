export const metadata = {
  title: "StockControl",
  description: "Sistema de controle de materiais",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
